# 驗證紀錄 — 2026-09-24

- `node --check`：預覽伺服器、場景操作程式、船艦建模程式均通過。
- 已在 Codex 內建瀏覽器開啟 `http://127.0.0.1:4317/`，實際查看 Three.js 渲染結果。
- 實際滑鼠操作確認：左鍵拖曳改變觀看角度、滾輪缩放、暫停自轉、隱藏與顯示船帆。
- 實際鍵盤操作確認：Tab／Shift+Tab 導覽、Enter 切換船帆，以及自訂選取樣式。
- 已檢查瀏覽器擷取的 console warning／error，當次查詢沒有紀錄。
- 手把輸入處理已加入，但尚未使用實體手把驗證。
- 此次未做 FPS 壓力測試，也未加入航行與戰鬥系統。

預覽服務只監聽本機 127.0.0.1；Three.js 已安裝於專案依賴，預覽時不依賴 CDN。

## 珊瑚灣小島新增驗證

- `node --check ship-preview/island.js` 與 `node --check ship-preview/main.js` 通過。
- 在內建瀏覽器實際查看正面及拖曳後的側後方，確認民居、燈塔、岩台、階梯、碼頭、礁石和水下珊瑚顯示。
- 實際點擊切換到單船預覽，再返回小島預覽。
- 新增場景的 console warning／error 查詢沒有紀錄。
- 尚未做實體手把或效能壓力測試；這是場景造型預覽。

## 小島海浪新增驗證

- `node --check` 通過：`main.js`、`island.js`、`ocean.js`、`waves.js`。
- 在內建瀏覽器實際確認波浪、水面高光、海岸與礁石周圍的動態泡沫顯示，並確認淺海珊瑚仍可辨識。
- 實際點擊「海浪：開／關」，確認外海浪峰與船身浮動可平順切換。
- 針對波高、岸邊波幅、船身姿態與關閉後回正執行計算檢查；取樣波高介於約 -0.45 至 +0.45 個場景單位。
- 海面網格為 25,921 頂點、51,200 三角形；海岸與礁石泡沫合併為一個幾何物件，兩者合計增加約 2 次繪製呼叫。這是幾何預算，實際效能仍以目標硬體測量為準。
- 在本機內建瀏覽器的一次約 3 秒取樣顯示約 60 FPS；尚未做多設備或長時間效能測試。
